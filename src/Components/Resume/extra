<Box d="flex" mt={10} backgroundColor="white" boxShadow="dark-lg">
        <SimpleGrid columns={{ base: "1", md: "1", lg: "2" }}>
          <Box
            d="flex"
            alignItems="center"
            justifyContent="center"
            backgroundColor="black"
            color="whitesmoke"
            py={3}
            width={{ base: "100%", md: "100%%", lg: "auto" }}
          >
            <List width="100%" p={8}>
              <ListItem
                backgroundColor={
                  section === "education" ? "whitesmoke" : "black"
                }
                p={2}
                borderRadius={10}
                color={section === "education" ? "black" : "whitesmoke"}
                onClick={() => setsection("education")}
              >
                <i class="fa-solid fa-user-graduate"></i>
                &nbsp;&nbsp;Education
              </ListItem>
              <ListItem
                backgroundColor={section === "work" ? "whitesmoke" : "black"}
                p={2}
                borderRadius={10}
                color={section === "work" ? "black" : "whitesmoke"}
                onClick={() => setsection("work")}
              >
                <i class="fa-solid fa-clock-rotate-left"></i>
                &nbsp;&nbsp;Work History
              </ListItem>
              <ListItem
                backgroundColor={section === "skills" ? "whitesmoke" : "black"}
                p={2}
                borderRadius={10}
                color={section === "skills" ? "black" : "whitesmoke"}
                onClick={() => setsection("skills")}
              >
                <i class="fa-solid fa-laptop-code"></i>
                &nbsp;&nbsp;Programming Skills
              </ListItem>
              {/* You can also use custom icons from react-icons */}
              <ListItem
                backgroundColor={
                  section === "projects" ? "whitesmoke" : "black"
                }
                p={2}
                borderRadius={10}
                color={section === "projects" ? "black" : "whitesmoke"}
                onClick={() => setsection("projects")}
              >
                <i class="fa-solid fa-code"></i>
                &nbsp;&nbsp;Projects
              </ListItem>
            </List>
          </Box>
          <Box
            d="flex"
            justifyContent="center"
            width={{ base: "90%", md: "90%", lg: "auto" }}
          >
            {section === "education" ? (
              <Education />
            ) : section === "work" ? (
              <Work />
            ) : section === "skills" ? (
              <Skills />
            ) : section === "projects" ? (
              <Projects />
            ) : (
              <></>
            )}
          </Box>
        </SimpleGrid>
      </Box>